package com.rafamv.bygoneage.handler;

import com.rafamv.bygoneage.BygoneAge;

import net.minecraft.entity.Entity;
import net.minecraft.world.World;

public class BygoneAgeSoundHandler {
	/*
	public static void onEntityPlay(String name, World world, Entity entityName, float volume, float pitch) {
		world.playSoundAtEntity(entityName, (BygoneAge.MODID + ":" + name), volume, pitch);
	}
	*/
}
